'use strict';function escapeHtml(value){return String(value).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;')}(()=>{const baseMetaTag=document.querySelector('meta[name="base-url"]');const analyticsBaseUrl=baseMetaTag?baseMetaTag.content:'';const startTs=Date.now();if(!analyticsBaseUrl){return}
function sendAnalytics(){const durationSec=Math.max(1,Math.round((Date.now()-startTs)/1000));const data=new URLSearchParams({page:window.location.pathname||'/',duration:String(durationSec),});const target=`${analyticsBaseUrl}/api/analytics`;if(navigator.sendBeacon){const blob=new Blob([data.toString()],{type:'application/x-www-form-urlencoded'});navigator.sendBeacon(target,blob);return}
fetch(target,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Accept':'application/json',},body:data.toString(),keepalive:!0,}).catch(()=>{})}
window.addEventListener('pagehide',sendAnalytics,{once:!0})})();(()=>{const favoriteButtons=document.querySelectorAll('.favorite-btn');const csrfTokenMeta=document.querySelector('meta[name="csrf-token"]');const baseUrlMeta=document.querySelector('meta[name="base-url"]');const favoriteCsrf=csrfTokenMeta?csrfTokenMeta.content:'';const favoriteBaseUrl=baseUrlMeta?baseUrlMeta.content:'';favoriteButtons.forEach((button)=>{button.addEventListener('click',()=>{const articleId=Number(button.getAttribute('data-article-id')||0);if(!favoriteBaseUrl||articleId<=0){return}
button.disabled=!0;fetch(`${favoriteBaseUrl}/api/favorite`,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded','Accept':'application/json','X-CSRF-Token':favoriteCsrf,},body:new URLSearchParams({csrf_token:favoriteCsrf,article_id:String(articleId),}),}).then((response)=>response.json()).then((payload)=>{if(!payload||payload.ok!==!0){return}
const isFavorite=Boolean(payload.favorite);const count=Number(payload.count||0);button.classList.toggle('active',isFavorite);button.textContent=isFavorite?`Retire de lire plus tard (${count})`:`Sauvegarder article (${count})`}).catch(()=>{}).finally(()=>{button.disabled=!1})})})})();(()=>{if(!('IntersectionObserver' in window)||'loading' in HTMLImageElement.prototype){return}
const images=document.querySelectorAll('img[loading="lazy"]');const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(!entry.isIntersecting){return}
const img=entry.target;if(img instanceof HTMLImageElement&&img.dataset.src){img.src=img.dataset.src}
observer.unobserve(entry.target)})});images.forEach((img)=>observer.observe(img))})();(()=>{const navToggle=document.querySelector('.nav-toggle');const navMenu=document.querySelector('.nav-menu');const dropdownToggles=document.querySelectorAll('.nav-dropdown-toggle');if(navToggle&&navMenu){navToggle.addEventListener('click',()=>{const isOpen=navMenu.classList.toggle('open');navToggle.setAttribute('aria-expanded',String(isOpen))})}
const closeDropdowns=()=>{document.querySelectorAll('.nav-dropdown.open').forEach((dropdown)=>{dropdown.classList.remove('open');const toggle=dropdown.querySelector('.nav-dropdown-toggle');if(toggle){toggle.setAttribute('aria-expanded','false')}})};dropdownToggles.forEach((toggle)=>{toggle.addEventListener('click',(event)=>{event.preventDefault();const dropdown=toggle.closest('.nav-dropdown');if(!dropdown){return}
const isOpen=dropdown.classList.contains('open');closeDropdowns();if(!isOpen){dropdown.classList.add('open');toggle.setAttribute('aria-expanded','true')}})});document.addEventListener('click',(event)=>{const target=event.target;if(!(target instanceof Node)){return}
if(navToggle&&navMenu&&!navToggle.contains(target)&&!navMenu.contains(target)){navMenu.classList.remove('open');navToggle.setAttribute('aria-expanded','false')}
if(!target.closest('.nav-dropdown')){closeDropdowns()}});document.querySelectorAll('[data-confirm]').forEach((button)=>{button.addEventListener('click',(event)=>{const message=button.getAttribute('data-confirm')||'Confirmer cette action ?';if(!window.confirm(message)){event.preventDefault()}})})})();(()=>{const mapElement=document.getElementById('fo-events-map');if(!mapElement||typeof window.L==='undefined'){return}
const map=L.map(mapElement).setView([32.0,53.0],5);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:18,attribution:'&copy; OpenStreetMap contributors',}).addTo(map);const typeColors={militaire:'#c62828',politique:'#1565c0',diplomatique:'#2e7d32',bombardement:'#ef6c00',manifestation:'#6a1b9a',};const clusterLayer=typeof L.markerClusterGroup==='function'?L.markerClusterGroup():L.layerGroup();clusterLayer.addTo(map);let heatLayer=null;const heatPoints=[];let lastValidLatLng=[];const iranZoneGeoJson={type:'Feature',geometry:{type:'Polygon',coordinates:[[[44.0,39.8],[47.0,39.0],[51.0,38.5],[56.5,36.8],[61.8,34.5],[62.5,30.0],[61.0,26.0],[56.0,25.0],[51.0,25.5],[47.5,27.0],[45.0,30.0],[44.0,35.0],[44.0,39.8],]],},};L.geoJSON(iranZoneGeoJson,{style:{color:'#90a4ae',fillColor:'#546e7a',fillOpacity:0.08,weight:1.5,},}).addTo(map);const renderEvents=(events)=>{clusterLayer.clearLayers();heatPoints.length=0;lastValidLatLng=[];events.forEach((event)=>{const lat=Number(event.latitude||0);const lng=Number(event.longitude||0);if(!Number.isFinite(lat)||!Number.isFinite(lng)||lat<-90||lat>90||lng<-180||lng>180){return}
const color=typeColors[event.type]||'#c62828';const marker=L.circleMarker([lat,lng],{radius:7,color,fillColor:color,fillOpacity:0.8,weight:1,});const popup=`
                <div class="map-popup">
                    <h3>${escapeHtml(String(event.title || 'Evenement'))}</h3>
                    <p><strong>Type:</strong> ${escapeHtml(String(event.type || ''))}</p>
                    <p><strong>Ville:</strong> ${escapeHtml(String(event.city || ''))}</p>
                    <p><strong>Date:</strong> ${escapeHtml(String(event.event_date || ''))}</p>
                    <p>${escapeHtml(String(event.description || ''))}</p>
                </div>
            `;marker.bindPopup(popup);clusterLayer.addLayer(marker);heatPoints.push([lat,lng,0.6]);lastValidLatLng.push([lat,lng])});if(heatLayer){map.removeLayer(heatLayer);heatLayer=null}
if(heatPoints.length>0&&typeof L.heatLayer==='function'){heatLayer=L.heatLayer(heatPoints,{radius:24,blur:18})}};const addHeatToggle=()=>{if(typeof L.heatLayer!=='function'){return}
const HeatControl=L.Control.extend({options:{position:'topright'},onAdd(){const button=L.DomUtil.create('button','leaflet-bar heat-toggle');button.type='button';button.textContent='Heatmap';button.setAttribute('aria-label','Basculer heatmap');L.DomEvent.on(button,'click',(event)=>{L.DomEvent.stopPropagation(event);if(!heatLayer){return}
if(map.hasLayer(heatLayer)){map.removeLayer(heatLayer)}else{heatLayer.addTo(map)}});return button},});map.addControl(new HeatControl())};const apiUrl=mapElement.dataset.apiUrl||'';if(apiUrl!==''){fetch(apiUrl,{headers:{Accept:'application/json'}}).then((response)=>response.json()).then((payload)=>{const events=Array.isArray(payload.data)?payload.data:[];renderEvents(events);addHeatToggle();if(lastValidLatLng.length>0){const bounds=L.latLngBounds(lastValidLatLng);if(bounds.isValid()){map.fitBounds(bounds.pad(0.15))}}}).catch(()=>{})}})();(()=>{const csrfMeta=document.querySelector('meta[name="csrf-token"]');const csrfToken=csrfMeta?csrfMeta.content:'';const baseMeta=document.querySelector('meta[name="base-url"]');const baseUrl=baseMeta?baseMeta.content:'';function pollNotifications(){fetch(`${baseUrl}/api/notifications`,{headers:{Accept:'application/json'},}).then((response)=>response.json()).then((payload)=>{if(!payload||payload.ok!==!0){return}
updateBadges(Number(payload.unread_count||0));const items=Array.isArray(payload.data)?payload.data:[];updateNotificationList(items);maybeTriggerWebNotification(items)}).catch(()=>{})}
function updateBadges(unreadCount){document.querySelectorAll('.notification-badge').forEach((badge)=>{badge.textContent=String(unreadCount);badge.hidden=unreadCount<=0})}
function updateNotificationList(items){const list=document.getElementById('notification-list');if(!list){return}
list.innerHTML='';const sliced=items.slice(0,8);if(sliced.length===0){const li=document.createElement('li');li.textContent='Aucune notification.';list.appendChild(li);return}
sliced.forEach((item)=>{const li=document.createElement('li');li.className=Number(item.is_read||0)===0?'notification-item unread':'notification-item';li.innerHTML=`
                <strong>${escapeHtml(String(item.type || 'info'))}</strong>
                <p>${escapeHtml(String(item.message || ''))}</p>
                <small>${escapeHtml(String(item.created_at || ''))}</small>
            `;list.appendChild(li)})}
function maybeTriggerWebNotification(items){if(!('Notification' in window)){return}
const unread=items.find((item)=>Number(item.is_read||0)===0);if(!unread){return}
const lastSeenId=window.localStorage.getItem('last_notification_id');if(lastSeenId===String(unread.id)){return}
const fireNotification=()=>{try{new Notification('Iran - Nouvelle notification',{body:String(unread.message||'Mise a jour disponible'),icon:`${baseUrl}/public/images/logo.webp`,});window.localStorage.setItem('last_notification_id',String(unread.id))}catch(error){}};if(Notification.permission==='granted'){fireNotification();return}
if(Notification.permission==='default'){Notification.requestPermission().then((permission)=>{if(permission==='granted'){fireNotification()}})}}
if(baseUrl){pollNotifications();window.setInterval(pollNotifications,60000)}
const markAllButton=document.getElementById('mark-all-notifications');if(markAllButton){markAllButton.addEventListener('click',()=>{fetch(`${baseUrl}/api/notifications/read`,{method:'POST',headers:{'X-CSRF-Token':csrfToken,Accept:'application/json',},body:new URLSearchParams({csrf_token:csrfToken}),}).then((response)=>response.json()).then(()=>pollNotifications()).catch(()=>{})})}})();(()=>{const searchInput=document.querySelector('.search-form input[type="search"]');const cards=document.querySelectorAll('.article-card');if(!searchInput||cards.length===0){return}
searchInput.addEventListener('input',()=>{const query=searchInput.value.trim().toLowerCase();cards.forEach((card)=>{const text=card.textContent?card.textContent.toLowerCase():'';card.style.display=text.includes(query)?'':'none'})})})();(()=>{const shareContainers=document.querySelectorAll('.share-buttons[data-article-id]');const csrfToken=document.querySelector('meta[name="csrf-token"]')?.getAttribute('content')||'';const baseUrlMeta=document.querySelector('meta[name="base-url"]')?.getAttribute('content')||'';const baseUrl=baseUrlMeta.replace(/\/+$/,'');const shareEndpoint=baseUrl?`${baseUrl}/share-log`:'share-log';shareContainers.forEach((container)=>{const articleId=Number(container.getAttribute('data-article-id')||'0');if(!Number.isInteger(articleId)||articleId<=0){return}
container.querySelectorAll('.share-btn').forEach((button)=>{button.addEventListener('click',async()=>{const channel=String(button.getAttribute('data-channel')||'').trim();if(!channel){return}
if(channel==='copy'){try{await navigator.clipboard.writeText(window.location.href);button.textContent='Lien copie';setTimeout(()=>{button.textContent='Copier le lien'},1600)}catch(error){}}
const form=new FormData();form.set('article_id',String(articleId));form.set('channel',channel);if(csrfToken){form.set('csrf_token',csrfToken)}
fetch(shareEndpoint,{method:'POST',body:form,credentials:'same-origin',headers:{'X-Requested-With':'XMLHttpRequest',},}).catch(()=>{})})})})})();(()=>{const chartCanvas=document.getElementById('fo-stats-chart');const series=window.__FO_STATS_SERIES__;if(!chartCanvas||typeof window.Chart==='undefined'||!series){return}
new window.Chart(chartCanvas,{type:'line',data:{labels:Array.isArray(series.labels)?series.labels:[],datasets:[{label:'Pertes humaines',data:Array.isArray(series.pertes)?series.pertes:[],borderColor:'#c62828',backgroundColor:'rgba(198,40,40,0.18)',tension:0.28,fill:!0,},{label:'Deplacements population',data:Array.isArray(series.deplacements)?series.deplacements:[],borderColor:'#1565c0',backgroundColor:'rgba(21,101,192,0.16)',tension:0.28,fill:!0,},{label:'Sanctions economiques',data:Array.isArray(series.sanctions)?series.sanctions:[],borderColor:'#2e7d32',backgroundColor:'rgba(46,125,50,0.16)',tension:0.28,fill:!0,},],},options:{responsive:!0,maintainAspectRatio:!1,interaction:{mode:'index',intersect:!1,},plugins:{legend:{labels:{color:'#dedede',},},},scales:{x:{ticks:{color:'#bdbdbd'},grid:{color:'rgba(255,255,255,0.08)'},},y:{ticks:{color:'#bdbdbd'},grid:{color:'rgba(255,255,255,0.08)'},},},},})})();(()=>{const timelineWrapper=document.getElementById('timeline-track-wrapper');const timelineTrack=document.getElementById('timeline-track');function revealTimelineCards(){const cards=document.querySelectorAll('.timeline-event-card');if(cards.length===0){return}
const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){entry.target.classList.add('is-visible')}})},{root:document.getElementById('timeline-track-wrapper'),threshold:0.35,});cards.forEach((card)=>observer.observe(card))}
if(!timelineWrapper||!timelineTrack){return}
timelineWrapper.addEventListener('wheel',(event)=>{if(Math.abs(event.deltaY)<=Math.abs(event.deltaX)){return}
event.preventDefault();timelineWrapper.scrollLeft+=event.deltaY},{passive:!1});timelineWrapper.addEventListener('keydown',(event)=>{if(event.key==='ArrowRight'){timelineWrapper.scrollLeft+=280}else if(event.key==='ArrowLeft'){timelineWrapper.scrollLeft-=280}});const apiUrl=timelineWrapper.dataset.apiUrl||'';if(apiUrl!==''){fetch(apiUrl,{headers:{Accept:'application/json'}}).then((response)=>response.json()).then((payload)=>{const events=Array.isArray(payload.data)?payload.data:[];if(events.length===0){return}
timelineTrack.innerHTML='';events.forEach((event)=>{const card=document.createElement('article');card.className='timeline-event-card';card.tabIndex=0;card.innerHTML=`
                        <span class="timeline-date">${escapeHtml(String(event.event_date || ''))}</span>
                        <span class="timeline-category">${escapeHtml(String(event.category || ''))}</span>
                        <h3>${escapeHtml(String(event.title || ''))}</h3>
                        <p>${escapeHtml(String(event.description || ''))}</p>
                    `;timelineTrack.appendChild(card)});revealTimelineCards()}).catch(()=>{revealTimelineCards()})}else{revealTimelineCards()}})();(()=>{const ttsButtons=document.querySelectorAll('.tts-toggle');let currentUtterance=null;ttsButtons.forEach((button)=>{button.addEventListener('click',()=>{if(!('speechSynthesis' in window)){button.textContent='TTS non supporte';return}
if(window.speechSynthesis.speaking){window.speechSynthesis.cancel();currentUtterance=null;button.textContent='Ecouter';return}
const targetSelector=button.getAttribute('data-target')||'';const target=targetSelector?document.querySelector(targetSelector):null;if(!target){return}
const text=target.textContent?target.textContent.trim():'';if(text.length<10){return}
currentUtterance=new SpeechSynthesisUtterance(text);currentUtterance.lang='fr-FR';currentUtterance.rate=1;currentUtterance.pitch=1;currentUtterance.onend=()=>{button.textContent='Ecouter';currentUtterance=null};currentUtterance.onerror=()=>{button.textContent='Ecouter';currentUtterance=null};button.textContent='Arreter';window.speechSynthesis.speak(currentUtterance)})})})()